packageMetadata:
  - repository: qlik-trial/fun-bag-agent
    type: docker
    name: fun-bag-agent
    apiComplianceConfig:
      specPaths:
        - {spec_path} ## TODO: Replace {spec_path} with actual path
    depConfig:
      pythonDepSpecs:
        - requirements.txt
  - repository: qlik-trial/fun-bag-agent
    type: helm
    name: helm/fun-bag-agent
    depConfig:
      helmDepSpecs:
        - manifests/chart/fun-bag-agent
